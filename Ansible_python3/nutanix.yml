# Cluster settings
clusters:
    prod-cluster:
        address: 'ntnx-548b1f14-a-cvm.local'
        port: 9440
        username: 'admin'
        password: '1qaz@WSX3edc'
        verify_ssl: False

# Global caching settings
caching:
    cache_max_age: 300
    #cache_path: '~/.ansible/tmp'
    cache_path: '/tmp'
    cache_base_name: 'ansible-nutanix.cache'

# Settings
settings:
    nested_groups: True
# Grouping options
grouping:
    group_by_instance_id: true
    group_by_instance_type: true
    group_by_instance_state: true
    group_by_platform: true